rules:
  # TODO: Consider adding a GitHub Environment to release.yml instead
  # once super-linter bundles zizmor >= v1.24.0 (per-secret allowlist).
  # See: secrets.PRIVATE_KEY in .github/workflows/release.yml
  secrets-outside-env: ignore
  unpinned-uses:
    config:
      policies:
        '*': ref-pin
